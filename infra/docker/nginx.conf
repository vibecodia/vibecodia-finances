log_format vhost_combined '$server_name - $remote_addr - $remote_user [$time_local] '
	'"$request" $status $body_bytes_sent '
        '"$http_referer" "$http_user_agent"';

# -------------------------
# VIBECODIA - REDIRECIONAMENTO HTTP PARA HTTPS
# -------------------------
server {
    listen 80;
    server_name vibecodia.com.br www.vibecodia.com.br;
    return 301 https://$host$request_uri;
}

# -------------------------
# VIBECODIA - CONFIGURAÇÃO PRINCIPAL HTTPS
# -------------------------
server {
    listen 443 ssl;
    server_name vibecodia.com.br www.vibecodia.com.br;

    ssl_certificate /etc/letsencrypt/live/vibecodia.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/vibecodia.com.br/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers off;

    # --- ALTERAÇÃO AQUI ---
    root /usr/share/nginx/html/vibecodia;
    index index.html;

    location /.well-known/acme-challenge/ {
        root /usr/share/nginx/html;
        allow all;
    }

    location / {
        # E AQUI, PARA APONTAR PARA O INDEX CORRETO
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://backend:3001/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    access_log /var/log/nginx/access.log vhost_combined;
}

# -------------------------
# ATELIE - REDIRECIONAMENTO HTTP PARA HTTPS
# -------------------------
server {
    listen 80;
    server_name atelie.vibecodia.com.br;
    return 301 https://$host$request_uri;
}

# -------------------------
# ATELIE - CONFIGURAÇÃO PRINCIPAL HTTPS
# -------------------------
server {
    listen 443 ssl;
    server_name atelie.vibecodia.com.br;

    ssl_certificate /etc/letsencrypt/live/vibecodia.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/vibecodia.com.br/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers off;

    # --- ALTERAÇÃO AQUI ---
    root /usr/share/nginx/html/atelie;
    index index.html;

    location /.well-known/acme-challenge/ {
        root /usr/share/nginx/html;
        allow all;
    }

    location / {
        # E AQUI, PARA APONTAR PARA O INDEX CORRETO
        try_files $uri $uri/ /index.html;
    }

    access_log /var/log/nginx/access.log vhost_combined;
}

# -------------------------
# FINANCES - REDIRECIONAMENTO HTTP PARA HTTPS
# -------------------------
server {
    listen 80;
    server_name finances.vibecodia.com.br;
    return 301 https://$host$request_uri;
}

# -------------------------
# FINANCES - CONFIGURAÇÃO PRINCIPAL HTTPS
# -------------------------
server {
    listen 443 ssl;
    server_name finances.vibecodia.com.br;

    ssl_certificate /etc/letsencrypt/live/vibecodia.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/vibecodia.com.br/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers off;

    # ESTA CONFIGURAÇÃO ESTÁ CORRETA E FOI MANTIDA
    root /usr/share/nginx/html;
    index index.html;
	
    location = /sw.js {
        root /usr/share/nginx/html;
        add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0";
        add_header Service-Worker-Allowed "/";
        expires off;
        proxy_no_cache 1;
    }

    location /.well-known/acme-challenge/ {
        root /usr/share/nginx/html;
        allow all;
    }

    location /api/ {
       proxy_pass http://backend:3001/api/;
       proxy_set_header Host $host;
       proxy_set_header X-Real-IP $remote_addr;
       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
       proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /api/health-check {
       proxy_pass http://backend:3001/api/health-check;
       proxy_set_header Host $host;
       proxy_set_header X-Real-IP $remote_addr;
       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
       proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    access_log /var/log/nginx/access.log vhost_combined;
}

#
# WEBIDS 
#

server {
    listen 80;
    server_name webids.vibecodia.com.br;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name webids.vibecodia.com.br;

    ssl_certificate /etc/letsencrypt/live/webids.vibecodia.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/webids.vibecodia.com.br/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers off;

    root /usr/share/nginx/html/webids;
    index index.html;

    location /.well-known/acme-challenge/ {
        root /usr/share/nginx/html;
        allow all;
    }

    location /api/ {
        proxy_pass http://webids_server:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    access_log /var/log/nginx/access.log vhost_combined;
}

# -------------------------
# PLAYGROUND - REDIRECIONAMENTO HTTP PARA HTTPS
# -------------------------
server {
    listen 80;
    server_name playground.vibecodia.com.br;
    return 301 https://$host$request_uri;
}

# -------------------------
# PLAYGROUND - CONFIGURAÇÃO PRINCIPAL HTTPS
# -------------------------
server {
    listen 443 ssl;
    server_name playground.vibecodia.com.br;

    ssl_certificate /etc/letsencrypt/live/playground.vibecodia.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/playground.vibecodia.com.br/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers off;

    location /.well-known/acme-challenge/ {
        root /usr/share/nginx/html;
        allow all;
    }

    # Proxy para a aplicação Go na porta 3016
    location / {
        proxy_pass http://vibecodia-cron-ai-app:3016;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    access_log /var/log/nginx/access.log vhost_combined;
}

# -------------------------
# API - REDIRECIONAMENTO HTTP PARA HTTPS
# -------------------------
server {
    listen 80;
    server_name api.vibecodia.com.br;
    return 301 https://$host$request_uri;
}

# -------------------------
# API - CONFIGURAÇÃO PRINCIPAL HTTPS
# -------------------------
server {
    listen 443 ssl;
    server_name api.vibecodia.com.br;

    ssl_certificate /etc/letsencrypt/live/api.vibecodia.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.vibecodia.com.br/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers off;

    location /.well-known/acme-challenge/ {
        root /usr/share/nginx/html;
        allow all;
    }

    # Proxy para a aplicação Go na porta 3016
    location / {
        proxy_pass http://fastapi_chatbot_prod:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    access_log /var/log/nginx/access.log vhost_combined;
}

