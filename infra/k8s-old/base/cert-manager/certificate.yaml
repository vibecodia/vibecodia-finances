apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vibecodia-tls
spec:
  secretName: vibecodia-tls
  dnsNames:
    - vibecodia.local
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
    group: cert-manager.io
